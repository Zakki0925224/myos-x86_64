#ifndef _TEMP_H
#define _TEMP_H

#define SEEK_SET 0
#define SEEK_END 2

typedef struct _FILE
{
} FILE;

// extern FILE *stdin;
// extern FILE *stdout;
// extern FILE *stderr;

// #define stdin stdin
// #define stdout stdout
// #define stderr stderr

#endif
